import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.stream.Collectors;

public class EY_Test {
	public static void main(String[] args) {
		List<Integer> l1 = List.of(5,2,6,4,1,3,7);
		
		//List<Integer> less_than_N = l1.stream().filter(ele -> {ele<=4}).collect(Collectors.toList());
		
		
		// Callable, Runnable, Executor Service
		
		// Java 8 all - groupingBy, Partioning
		
		// Design Pattern / Solid Principal - Builder, Singleton, Abstract.
		
		// Singleton
		
		// Any MQ
		
		// Immutable Class / Object
		
		// JPA - Stateless / Locking / Transaction
		
		// Collection - Concurrent HashMap
		
		//
		
		// How to add two condition by using Java8
		
		// Find common element between two list by using java 8.
		
		//
		
		//List<Integer> l2 = List.of(9,3,1,5);
		
		//Set<Integer> set = new HashSet<>();
		
		//Optional<Integer> findFirst = l1.stream().filter(ele -> !set.add(ele)).findFirst();
		
//		for(Integer i1 : l2)
//		{
//			if(l1.contains(i1))
//			{
//				System.out.println((int)i1);
//				break;
//			}
//			l1.add(i1);
//		}
		
	}
}
